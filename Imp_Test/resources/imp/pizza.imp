namespace test {

	enum Crust {
		crispy,
		soft
		burnt = 3
		missing
	}

	class Pizza {
		toppings:string[]

		add(topping:string) {
			var x = 0
			if (topping == null)
				return
			else
				x = 1
				

			toppings.add(topping)
		}
	}
}