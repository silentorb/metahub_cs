@@@contains(list, item)
if (item != null && list.contains(item) == false) {
	list += item
}
@@@equals(first, second)
first = second
@@@map_on_add(T, ref, list, hub)
if (ref != null) {
	var item:T = new T()
	item.initialize(hub)
	list.add(item)
}